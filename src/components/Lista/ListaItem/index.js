import React from "react";

import { View } from "react-native";

// import styles from './styles';

const ListaItem = ({ id, title, description, avatar, handleClick }) => <View />;

export default ListaItem;
